
def QuickSort():
    if(low>high):
        pivot = self.partition(self.array_a, self.low, self.high)
        self.QuickSort(self.array_a, self.high,self.pivot)
        self.QuickSort(self.array_a, self.pivot+1,self.high)
def swap(self,a,b):
    pass

def partition(arr, low, high):
    while(low<high):
        




    